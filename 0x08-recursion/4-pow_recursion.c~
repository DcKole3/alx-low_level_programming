#include "main.h"
/**
 * factorial - returns factorial of a number.
 * @n: string to print in reverse
 * Return: returns factorial or -1 (error)
 */
int factorial(int n)
{
if (n < 0)
{
return (-1);
}
if (n > 0)
{
return (n * factorial(n - 1));
}
return (1);
}
