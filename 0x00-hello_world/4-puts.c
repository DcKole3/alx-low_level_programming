#include <stdio.h>
/**
 * main  - code entry point
 * Return: 0 = Success, otherwise EOF = error
*/
int main(void)
{
const char *_s = "\"Programming is like building a multilingual puzzle";
puts(_s);
return (0);
}
