#include <stdio.h>
/**
 * main  - code entry point
 *return: Always 0 = Success, otherwise EOF = error
*/
int main(void)
{  
  const char *_s="\"Programming is like building a multilingual puzzle"; /*Declares s as a variable*/
  puts(_s);  /*writes the string s and a trailing newline*/
  return (0);
}
