#include "main.h"
/**
 * _puts_recursion - prints string
 * @s: string to print recursively
 */
void _puts_recursion(char *s)
{
  _putchar(*s);
  if (*s == '\0')
    {
      _putchar('\n');
    }
  _puts_recursion(++s);
}
